{
    "version": "0.2.0",
    "configurations": [
        {
            "name": "Launch aiwolf-nlp-server",
            "type": "go",
            "request": "launch",
            "mode": "debug",
            "program": "${workspaceFolder}/main.go",
            "args": [
                "-c",
                "${workspaceFolder}/config/debug.yml"
            ],
        }
    ]
}